import { Select, TextBoxValidator, CheckBoxList, TextArea } from '../components';

const components = {
  textBoxValidator: TextBoxValidator,
  textarea: TextArea,
  select: Select,
  checkBoxList: CheckBoxList,
};

export default components;
